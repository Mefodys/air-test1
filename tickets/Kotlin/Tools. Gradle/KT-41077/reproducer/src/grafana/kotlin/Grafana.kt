package grafana

// Deliberately invalid if compiled. A successful `build` demonstrates that the
// source set is not connected to the JVM compilation, as described by KT-41077.
val mustNotBeCompiled: DoesNotExist = TODO()
